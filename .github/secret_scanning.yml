# Test fixtures intentionally contain realistic-looking but fake credentials
# to verify that the anonymizer catches them. These are not real secrets.
paths-ignore:
  - "tests/fixtures.py"
  - "scripts/dev/live_test.py"
